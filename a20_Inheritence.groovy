class a20_Inheritence {
    // this would be the parent class
    // the properties in this class are shared by the children
    String venture;
    String wonky;
    String precarious;

    // function that is common to all
    def brazen (){
        println ("brazen means obvious and bold.$venture means to go on some risky journey; also, noun - risky journey")
    }
    def facetious (){
        println("facetious means making fun of something not appropriate, some serious issue.While $wonky means crooked or offcenter")
        }

    def compel () {
        println("compel means to force to do somethin. $precarious means unstable or uncertain ")
    }
}
